// TODO: Fix AJAX together with PHP
// fetch("link.php")
//     .then(response => response.json())
//     .then(profiles => {
//         // Loop through the array of profile data and create a new card element for each profile
//         profiles.forEach(profile => {
//             const card = document.createElement("div");
//             card.classList.add("card");
//             card.innerHTML = `
//         <h3>${profile.name}</h3>
//         <p>Age: ${profile.age}</p>
//       `;

//             // Add the card element to the page
//             document.getElementById("app").appendChild(card);
//         });
//     });